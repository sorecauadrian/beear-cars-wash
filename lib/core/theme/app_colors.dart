import 'package:flutter/material.dart';

/// Brand colors for Beear Cars Wash
class AppColors {
  AppColors._();

  // Primary brand color (dark blue from logo)
  static const Color primary = Color(0xFF00395E);
  
  // Secondary color (white)
  static const Color secondary = Colors.white;
  
  // Additional colors
  static const Color background = Colors.white;
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFB00020);
  static const Color onPrimary = Colors.white;
  static const Color onSecondary = Color(0xFF00395E);
  static const Color onSurface = Color(0xFF00395E);
  static const Color onError = Colors.white;
  
  // Status colors for bookings
  static const Color requested = Color(0xFFFF9800); // Orange
  static const Color accepted = Color(0xFF2196F3); // Blue
  static const Color rejected = Color(0xFFF44336); // Red
  static const Color inProgress = Color(0xFF9C27B0); // Purple
  static const Color done = Color(0xFF4CAF50); // Green
}

